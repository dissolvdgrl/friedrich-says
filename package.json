{
  "name": "friedrichsays",
  "version": "1.0.0",
  "description": "Your daily dose of Friedrich Nietzsche quotes",
  "author": "Christie Hill",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "prod": "nuxt build && nuxt generate && pm2 restart ./ecosystem.config.js"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.3.6",
    "@nuxtjs/dotenv": "^1.4.0",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "node-sass": "^7.0.0",
    "sass-loader": "^8.0.2"
  }
}
